<!DOCTYPE html>
<html>
<head>
  <title>Online Shop</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class='header' >
  <%= render 'layouts/navbar' %>
  <div class="container">
    <div class='pull-left'>
      <%= link_to 'OnlineShop', root_path, class: 'navbar-brand pull-left' %><br/>
      <span class='pull-left'>Your e-commerce solution</span>
      <div style="clear:both;"></div>
    </div>
  </div>
</div>
<div class='shop-navigation'>
  <div class='container'>
    <% MainCategory.all.each do |x| %>
      <div><%= link_to x.name.upcase, x, class: ('active' if @main_category.name == x.name if @main_category.present?) %></div>
    <% end %>
  </div>
</div>

<div class='container'>
  <div class='row'>
    <div class='col-md-12'>
      <%= render 'layouts/flash_message' %>
      <%= yield %>
    </div>
  </div>
</div>

</body>
</html>