<%= form_for [:admin, @discount] do |f| %>
  <% if @discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discount.errors.count, "error") %> prohibited this discount from being saved:</h2>

      <ul>
      <% @discount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :since_when %><br>
    <%= f.date_select :since_when %>
  </div>
  <div class="field">
    <%= f.label :until_when %><br>
    <%= f.date_select :until_when %>
  </div>
  <div class="field">
    <%= f.hidden_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
